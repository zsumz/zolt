[project]
name = "platform-consumer"
version = "0.1.0"
group = "com.acme"
java = "21"
main = "com.acme.app.Main"

[repositories]
fixture = "https://fixture.example.test/repo"

# The Acme platform BOM supplies the managed version for every Acme dependency.
[platforms]
"com.acme.platform:acme-bom" = "1.0.0"

# Declared version-less: the version is provided by the imported BOM above.
[dependencies]
"com.acme:acme-http" = {}
