# zolt-repository

Fetches Maven repository metadata and artifacts, manages the local artifact
cache, provides artifact bytes for lockfile-pinned SHA-256 integrity, and
coordinates concurrent downloads.

This is an internal workspace module. User-facing workflows are documented in
the root [README](../../README.md) and [USAGE.md](../../USAGE.md).
