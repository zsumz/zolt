# zolt-test-model

Defines shared test runtime models for selectors, suites, shards, profiling
history, worker pools, and test execution summaries.

This is an internal workspace module. User-facing workflows are documented in
the root [README](../../README.md) and [USAGE.md](../../USAGE.md).
