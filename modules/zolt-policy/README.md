# zolt-policy

Evaluates dependency policy, baseline constraints, and package graph diagnostics
used by `zolt policy` and quality checks.

This is an internal workspace module. User-facing workflows are documented in
the root [README](../../README.md) and [USAGE.md](../../USAGE.md).
