# zolt-build

Owns Zolt's local build execution: source and resource discovery, compilation,
incremental fingerprints, package assembly, runtime launch, package evidence,
and build metadata.

This is an internal workspace module. User-facing workflows are documented in
the root [README](../../README.md) and [USAGE.md](../../USAGE.md).
