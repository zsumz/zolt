# zolt-model

Holds shared value objects for projects, dependencies, lockfiles, classpaths,
workspaces, command configuration, provenance, and structured errors.

This is an internal workspace module. User-facing workflows are documented in
the root [README](../../README.md) and [USAGE.md](../../USAGE.md).
