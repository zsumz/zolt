# zolt-toml

Parses and writes `zolt.toml`, workspace configuration, command tasks,
resources, generated sources, test settings, and `zolt.lock`.

This is an internal workspace module. User-facing workflows are documented in
the root [README](../../README.md) and [USAGE.md](../../USAGE.md).
